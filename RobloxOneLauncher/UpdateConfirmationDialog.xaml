<Window x:Class="RobloxOneLauncher.UpdateConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roblox One update" Width="640" Height="650"
        MinWidth="440" MinHeight="420" MaxWidth="820" MaxHeight="820"
        ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI Variable Text"
        UseLayoutRounding="True" ShowInTaskbar="False">
    <Border Margin="16" Padding="24" CornerRadius="18"
            Background="{StaticResource PanelBrush}"
            BorderBrush="#293042" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="UpdateTitleText" Foreground="White"
                           FontSize="24" FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
                <TextBlock Text="Review the signed release details before installing."
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="13" Margin="0,6,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Border Grid.Row="1" Margin="0,18,0,0" Padding="16,13"
                    Background="#0D1017" CornerRadius="12"
                    BorderBrush="#242B3A" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="SIGNED RELEASE VERIFIED"
                                   Foreground="#55D6A2" FontSize="11"
                                   FontWeight="Bold"/>
                        <TextBlock x:Name="PublishedText"
                                   Foreground="#C8D0DE" FontSize="12"
                                   Margin="0,5,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="SizeText" Grid.Column="1"
                               Foreground="{StaticResource MutedBrush}"
                               FontSize="12" Margin="18,0,0,0"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <TextBlock Grid.Row="2" Text="RELEASE NOTES"
                       Foreground="#7F8BA0" FontSize="11"
                       FontWeight="Bold" Margin="0,18,0,8"/>

            <Border Grid.Row="3" MinHeight="120" Padding="14"
                    Background="#0D1017" CornerRadius="12"
                    BorderBrush="#242B3A" BorderThickness="1">
                <TextBox x:Name="ReleaseNotesBox"
                         IsReadOnly="True" IsReadOnlyCaretVisible="True"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Background="Transparent" BorderThickness="0"
                         Padding="2" Foreground="#E5E9F0"
                         FontSize="13" TextBlock.LineHeight="20"
                         AutomationProperties.Name="Signed release notes"/>
            </Border>

            <TextBlock x:Name="IntegrityText" Grid.Row="4"
                       Foreground="{StaticResource MutedBrush}" FontSize="11"
                       Margin="0,14,0,0" TextWrapping="Wrap"/>

            <Grid Grid.Row="5" Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="App files are replaced; local account data stays outside the install folder."
                           Foreground="{StaticResource MutedBrush}" FontSize="11"
                           VerticalAlignment="Center" TextWrapping="Wrap"
                           Margin="0,0,18,0"/>
                <Button Grid.Column="1" Content="Cancel"
                        Click="CancelButton_Click" IsCancel="True"
                        Background="#252C3B" Padding="18,9"
                        Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Install update"
                        Click="InstallButton_Click" IsDefault="True"
                        Padding="22,9"
                        AutomationProperties.Name="Install verified update"/>
            </Grid>
        </Grid>
    </Border>
</Window>
